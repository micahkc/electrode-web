include "common.fbs";

namespace electrode.events;

enum Severity : ubyte {
  Info = 0,
  Warning = 1,
  Error = 2
}

table Event {
  header:electrode.common.Header;
  severity:Severity;
  code:string;
  message:string;
}

root_type Event;

